# 生图 API 密钥骨架（模板：复制为 .env 后填值；.env 已被仓库 gitignore，勿提交）
#
# 说明：当前引擎的板图生成走「宿主文生图」通道（宿主模型执行 prompts 生成 PNG，
# 引擎只写 prompt、收图、检查、渲染），本文件为将来直接接入生图 API 的预留位。

# 阿里云百炼（万相文生图 / qwen-vl 打分），预留
DASHSCOPE_API_KEY=

# 火山方舟（即梦 Seedream / DreamO 角色保持），预留
ARK_API_KEY=
