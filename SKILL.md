---
name: kairos-whiteboard-video
description: >
  Knowledge → Whiteboard Video compiler. Use when turning knowledge, mechanisms,
  comparisons, processes, or arguments into a progressive whiteboard explainer
  with narration, drawing order, and audio/video synchronization.
compatibility: Python 3.11+, uv, FFmpeg, network access for Edge TTS. Board images are generated by the host image model.
metadata:
  version: "1.1"
---

# kairos-whiteboard-video

把知识编译成「边讲边画、逐步揭示、音画同步」的白板视频。

> Core model: knowledge → cognitive transitions → expression → timeline → deterministic rendering.

本 Skill 不负责生成板图。宿主模型负责出图；引擎负责校验、版式、时间轴、绘制和合成。涉及跨幕人物时，Skill 先建立项目级 Canonical Character Sheet，再把它作为场景图的主参考输入。

## 1. 不可破坏的系统不变量

1. **认知先于制作**：先确定观众需要完成的认知转变，再决定 Scene / Beat / 画面。
2. **Beat 是认知单位，Scene 是画布容器**：Beat 数由不可合并的 Transition 决定，不由目标时长或固定幕数决定。
3. **真实语音是时间权威**：成片时间来自 words.json 的真实词级边界；字数只能用于前期预算。
4. **视觉渐进揭示**：画面状态随认知状态增长；禁止无理由提前展示关键结论。
5. **职责最小化**：每个视觉元素必须有认知职责；装饰性元素删除。
6. **单一事实源**：input/script.json 是内容 IR；words.json 是语音时间事实；theme 包是视觉约束；state.json 只记录流程状态。
7. **确定性下游**：IR 通过后，下游只消费上游产物，不重新解释知识或创造新的认知步骤。
8. **失败显式化**：任何降级都必须写入报告并在正式交付前阻断；禁止静默 fallback。
9. **角色与手素材稳定**：角色通过 Character Bible + canonical reference 管理；手由主题 canonical asset 提供，板图不得生成手。
10. **三次质量门**：confirm-content → confirm-visual → confirm-final。只有最后一关通过才可交付。

完整原则见：
- references/whiteboard-video-principles.md
- references/cognitive-timeline.md
- references/quality-checklist.md

## 2. 生产管线

~~~text
Knowledge
   ↓
Knowledge Model
   ↓
Learner Model / Transitions
   ↓
Expression Plan
   ↓
Scene + Beat Timeline
   ↓
Board Prompt → Host Image Model
   ↓
Import / Layout / Annotation
   ↓
TTS → words.json
   ↓
Progressive Render
   ↓
Assemble
   ↓
Validate
   ↓
Final Video
~~~

### Stage A — Content

输入可以是一句话、文章、机制或现象。

必须形成：
- knowledge_model
- learner_model / transitions
- scenes[].beats[]
- expression / visual reveal 计划

入口：

~~~bash
cd engine && uv run python scripts/workflow.py lint --episode-dir ../projects/<episode>
~~~

lint 有 blocking findings 时不得进入板图阶段。

### Stage B — Visual

涉及跨幕人物时，先执行一次角色母版流程：

```bash
uv run python scripts/workflow.py character-sheet --episode-dir ../projects/<episode> --theme ../themes/<theme>
# 宿主图像模型只生成 1 张 canonical character sheet
uv run python scripts/workflow.py import-character-sheet --episode-dir ../projects/<episode> --image <character-sheet.png>
```

母版固定后，再生成场景 prompts / boards。


~~~bash
cd engine
uv run python scripts/workflow.py prompts --episode-dir ../projects/<episode> [--theme ../themes/<theme>]
# 使用宿主图像模型批量生成 boards/
uv run python scripts/workflow.py import --episode-dir ../projects/<episode> --boards-dir ../projects/<episode>/build/boards [--theme ../themes/<theme>]
uv run python scripts/build_video.py --episode-dir ../projects/<episode> layout
uv run python scripts/build_video.py --episode-dir ../projects/<episode> annotate
uv run python scripts/workflow.py confirm-visual --episode-dir ../projects/<episode>
~~~

板图契约：
- 1920×1080 / 16:9
- 不包含可读文字
- 不包含手
- 角色遵守项目 Character Bible
- 每个元素能映射到认知职责
- layout 与 elements 一一对应
- 不允许静默降级

### Stage C — Audio / Render / Delivery

~~~bash
cd engine
uv run python scripts/workflow.py voice --episode-dir ../projects/<episode>
uv run python scripts/workflow.py render --episode-dir ../projects/<episode> --jobs 4
uv run python scripts/build_video.py --episode-dir ../projects/<episode> assemble
uv run python scripts/workflow.py validate --episode-dir ../projects/<episode> --full
uv run python scripts/workflow.py confirm-final --episode-dir ../projects/<episode>
~~~

## 3. 用户体验规则

- **批量而不是逐幕确认**：全部板图一次生成、一次审查。
- **低风险步骤自动化**：TTS、layout、annotation、render、validate 不增加额外人工确认。
- **局部修改优先**：修改一幕只重跑该幕及其下游依赖，不重做整期。
- **状态可解释**：status 必须明确当前质量门、阻断原因和下一步。
- **可恢复**：中断后从已有输入和 build 产物继续，不依赖隐式内存状态。
- **发布人工执行**：Skill 不代替用户发布。

## 4. 工程边界

仓库分为四层：

| Layer | 目录 | 职责 |
|---|---|---|
| Contract | references/, SKILL.md | 原理、IR、质量标准 |
| Compiler | engine/scripts/whiteboard_story/ | IR 校验、规划、标注、渲染、QA |
| Theme | themes/ | 风格、角色、手素材、渲染约束 |
| Episode | projects/<id>/ | 单期输入、中间产物、交付物 |

单期原则：

~~~text
input        = 用户确认的事实 / 决策
build        = 可重生中间产物
deliverables = 最终交付
~~~

不要把单期生成物提交到仓库。

架构与维护约束见 references/architecture.md。

## 5. 开发与测试

~~~bash
cd engine && uv sync
cd engine && uv run pytest -q
~~~

推荐验证顺序：

~~~text
unit tests
  ↓
lint / contract checks
  ↓
single-scene render
  ↓
full render
  ↓
assemble
  ↓
validate --full
  ↓
human contact-sheet review
~~~

不要用一次完整出片来替代单元测试，也不要用单元测试替代最终感知质检。

## 6. 文档路由

- 原理 / Beat / AI 味 → references/whiteboard-video-principles.md
- 知识建模 → references/knowledge-model.md
- Transition / Beat → references/learner-model.md
- 怎么说、怎么画 → references/expression-plan.md
- 时间轴 → references/cognitive-timeline.md
- 工程实现 → references/engine-design.md
- 渲染内核 → references/kernel-design.md
- 角色 / 主题 → references/theme-onboarding.md
- 交付质检 → references/quality-checklist.md
- IR 迁移 → references/ir-alignment.md
- 仓库结构、质量目标、测试策略 → references/architecture.md

## 7. 当前能力边界

- 板图质量受宿主图像模型影响；引擎通过 prompt contract、reference image 和自动检查降低不确定性。
- 跨幕人物必须经过 Character Bible、reference image 和 scene 引用校验；生成模型仍不能承诺像素级一致，因此最终一致性以 contact sheet 人审为准。
- 当前正式主题以 themes/registry.json 为准；registry 中显式标记的 default 主题会作为 prompts 的默认主题，用户仍可显式覆盖。
- 渲染内核以增量揭示为主；未实现的操作不得进入正式表达计划。
- Agent 不负责发布。
